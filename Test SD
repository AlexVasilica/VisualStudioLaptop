#include <iostream>
#include <algorithm>

using namespace std;

int n, v[100], s, i;

int main()
{
	cin >> n >> s;
	for (i = 1; i <= n; i++)
		cin >> v[i];
	sort(v + 1, v + n + 1);
	cn = n;
	for (i = 1; i <= n; i++)
	{
		if (v[i] + v[n] > s)
			n--;
		if (v[i] + v[n] < s)
	}

	return 0;
}
